PRIME DIRECTIVE:
Keep this file small enough to be usable under attention constraints.
If a single note becomes too long to use, split it into smaller linked notes.

HOT ROUTING:
If `PLAN.md` exists, read it before broad cross-cutting work.
For all other routing, use `docs/attention-management.md`.

Keep reusable protocols in skills, not here.
